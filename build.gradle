plugins {
    id 'java'
    id 'org.springframework.boot' version "${springBootVersion}" apply false
    id 'com.osacky.doctor' version '0.9.0'
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17)) // For JDK 17
    }
}

allprojects {
    
}

dependencies {
    implementation(project(':reveila'))
    implementation(project(':api'))
    implementation(project(':app'))
    //implementation(project(':vue-project'))

    //implementation("com.android.tools.build:gradle:8.4.0")
    //implementation("com.facebook.react:react-native-gradle-plugin:0.80.2")
    //implementation("org.jetbrains.kotlin:kotlin-gradle-plugin:2.1.20")
}
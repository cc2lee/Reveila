# system-home\standard\infrastructure\.env.prod

# To merge the sandbox and prod files using this variable instead of the command line, 
# remember that on Windows, you separate them with a semicolon:
# COMPOSE_FILE=docker-compose.prod.yml;sandbox/docker-compose.sandbox.yml
COMPOSE_FILE=docker-compose.prod.yml

POSTGRES_USER=admin
POSTGRES_PASSWORD=6788565119
